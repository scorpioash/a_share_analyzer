version: '3.8'

services:
  a_share_analyzer:
    build: .
    container_name: a_share_analyzer
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - AK_NET_MODE=keep
      - AK_DNS_HIJACK=1
    restart: unless-stopped
